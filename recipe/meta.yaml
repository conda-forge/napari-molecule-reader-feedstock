{% set name = "napari-molecule-reader" %}
{% set version = "0.1.3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/{{ name | replace('-', '_') }}-{{ version }}.tar.gz
  sha256: cf311b13392ad528d7fc2cdb186b607f81adea88e35c5baf2696fb1dd02960af

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python {{ python_min }}
    - setuptools-scm
  run:
    - atomium
    - numpy
    - pandas
    - python >={{ python_min }}
    - scipy

test:
  imports:
    - napari_molecule_reader
  commands:
    - pip list
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/brisvag/napari-molecule-reader
  summary: A napari plugin that read molecular structure files.
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - brisvag
    - goanpeca
    - jaimergp
